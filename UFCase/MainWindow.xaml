<Window
    x:Class="UFCase.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UFCase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
        <Grid.Resources>
            <!-- This top margin is the height of the custom TitleBar -->
            <Thickness x:Key="NavigationViewContentMargin">0,48,0,0</Thickness>
            <Thickness x:Key="NavigationViewContentGridBorderThickness">0</Thickness>
            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent"></SolidColorBrush>
        </Grid.Resources>
        
        <Border x:Name="AppTitleBar"
                IsHitTestVisible="False"
                VerticalAlignment="Top"
                Background="Transparent"
                Height="40"
                Canvas.ZIndex="1" 
                Margin="48,8,0,0">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="AppTitle"
                    Text="UFCase"
                    VerticalAlignment="Center"
                    Margin="12, 0, 0, 0"
                    Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Border>

        <NavigationView x:Name="NavView"
                        IsTitleBarAutoPaddingEnabled="False"
                        IsBackEnabled="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
                        BackRequested="NavView_BackRequested"
                        ItemInvoked="NavView_ItemInvoked"
                        Loaded="NavView_Loaded"
                        DisplayModeChanged="NavView_DisplayModeChanged"
                        Canvas.ZIndex="0">

            <NavigationView.PaneCustomContent>
                <ComboBox x:Name="ImageComboBox" HorizontalAlignment="Stretch" Margin="10,5,10,5"
                          SelectedIndex="{x:Bind ImageProv.SelectedIndex, Mode=TwoWay}" ItemsSource="{x:Bind ImageProv.Images}">
                    <ComboBox.Resources>
                        <TeachingTip x:Key="DefaultTip" Target="{x:Bind ImageComboBox}"
                                     Title="Switch Windows Image"
                                     Subtitle="You can specify Online or Offline image here! All the contents will be reloaded after switching and the changes you made will be lost. Take care of it!">
                            <TeachingTip.IconSource>
                                <SymbolIconSource Symbol="Switch" />
                            </TeachingTip.IconSource>
                        </TeachingTip>
                    </ComboBox.Resources>
                    <ComboBox.ItemTemplate>
                        <DataTemplate x:DataType="local:ImageItem">
                            <StackPanel Orientation="Horizontal" Height="Auto" HorizontalAlignment="Stretch">
                                <ImageIcon Source="{x:Bind Icon}" VerticalAlignment="Stretch" Width="35" Margin="0,7,7,7" />
                                <StackPanel Orientation="Vertical">
                                    <TextBlock Text="{x:Bind Version}" Style="{StaticResource SubtitleTextBlockStyle}" />
                                    <TextBlock Style="{StaticResource BodyTextBlockStyle}">
                                        <Run Text="{x:Bind Type}" />
                                        <Run Text="{x:Bind Bootdrive}" />
                                    </TextBlock>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </NavigationView.PaneCustomContent>
            
            <NavigationView.MenuItems>
                <NavigationViewItem Content="SysInfo" Icon="Admin" />
                <NavigationViewItem Content="Features" Icon="AllApps" />
                <NavigationViewItem Content="Optionals" Icon="Add" />
                <NavigationViewItem Content="Packages">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE950;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            
            <Frame x:Name="ContentFrame" />
        </NavigationView>
    </Grid>
</Window>
