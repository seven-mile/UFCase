<ContentDialog
    x:Class="UFCase.AddSourceContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UFCase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    Title="Add source"
    PrimaryButtonText="Add"
    SecondaryButtonText="Cancel">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="SourceTextBox" Grid.Column="0" PlaceholderText="input installing source dir" />
        <Button Margin="5,0,0,0" Grid.Column="1" Style="{StaticResource AccentButtonStyle}" Click="BrowserButton_Click" Content="..." />
    </Grid>
</ContentDialog>
