<Page
    x:Class="UFCase.FeaturesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UFCase"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Disabled">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="700" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Margin="50,70,50,20" BorderBrush="{ThemeResource TextControlBorderBrush}" BorderThickness="1">
            <TreeView SelectionMode="Single" CanReorderItems="False" AllowDrop="False" CanDragItems="False"
                      ItemsSource="{x:Bind FeatureDataSource}" BorderBrush="Aqua" BorderThickness="3">
                <TreeView.ItemTemplate>
                    <DataTemplate x:DataType="local:FeatureTreeElement">
                        <TreeViewItem ItemsSource="{x:Bind Children}" ToolTipService.ToolTip="{x:Bind Description}">
                            <StackPanel Orientation="Horizontal">
                                <CheckBox IsChecked="{x:Bind IsEnabled}" IsEnabled="True" Content="{x:Bind Name}" />
                                <!--<TextBlock VerticalAlignment="Center" Text="{x:Bind Name}" />-->
                            </StackPanel>
                        </TreeViewItem>
                    </DataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
            
        </Border>
    </Grid>
</Page>
